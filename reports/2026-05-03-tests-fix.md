# /qa-tests-fix — 2026-05-03

**Result:** 210 passed, 27 skipped
**Rounds needed:** 3

## Fixes applied

- `src/lib/env.ts` — empty-string env vars (`UPSTASH_REDIS_REST_URL`, `NEXT_PUBLIC_SENTRY_DSN`) wrapped with `z.preprocess(v => v === '' ? undefined : v, ...)` to avoid URL validation failure during build
- `tests/expiry-first-f26.spec.ts:123` — `locator('main, [data-main], body').textContent()` → `locator('main').first().textContent()` (strict mode: body+main both matched)
- `tests/kitchen-modifiers-f74-f79.spec.ts` — replaced 800ms static wait with `page.waitForResponse` for PATCH (before reload) and GET (after reload) to fix race condition where preference save wasn't guaranteed before page reload
- `tests/expiry-first-f26.spec.ts` — test now self-seeds an expiring pantry item via API before asserting (feature only renders when expiring items exist)
- `tests/kitchen-modifiers-f74-f79.spec.ts` — changed `waitUntil: 'networkidle'` to `domcontentloaded` to avoid timeout from ongoing recipe generation requests

## Root causes

- Environment variable validation failure (empty string vs undefined): 1 issue
- Playwright strict mode / multi-element locator: 1 test
- Race condition (async state update timing): 1 test
- Missing test data precondition (feature only renders when data exists): 1 test
- Navigation timeout from long-running streaming requests: 1 test
