# Token Hygiene Pass — ingredientbot.com (Run 2)

Date: 2026-05-10
Branch: staging
Result: FIXED — 2 focus-visible violations

## Audit Summary

| Category | Count | Action |
|---|---|---|
| Hardcoded hex/RGB | 0 violations | All hex usage is in legitimate locations (see run 1) |
| Raw Tailwind palette (non-print) | 0 | All `print:*` overrides in print-recipe-view are intentional |
| `bg-white` / `text-black` (solid) | 0 | All `bg-white/N` are intentional opacity overlays in cooking-mode dark UI |
| Missing `dark:` variants | 0 | Codebase uses semantic tokens throughout |
| Missing focus-visible rings | 2 fixed | See below |
| Forbidden inline styles | 0 | All `style={{}}` usage is dynamic (collection colors, progress %, grid cols) |

## Files Modified

### `src/components/settings/dietary-profile-section.tsx`
- Added `focus-visible:outline-none focus-visible:ring-1 focus-visible:ring-ring` to the disliked-ingredient remove button (inline X icon inside Badge).

### `src/components/recipe/history-client.tsx`
- Added `focus-visible:outline-none focus-visible:ring-1 focus-visible:ring-ring rounded` to tag filter buttons in the recipe history card expanded view.

## Flagged (not fixed — intentional)

- `src/components/recipe/print-recipe-view.tsx:204` — `border-gray-300 text-gray-400` in a `hidden print:block` footer. These apply only when printing; file has `// design-system-exempt` annotation. Preserved.
- `src/app/global-error.tsx` — inline hex styles required (renders before Tailwind loads).

## Verification

- `npx prisma generate` — OK
- `npx tsc --noEmit` — clean (no errors)
