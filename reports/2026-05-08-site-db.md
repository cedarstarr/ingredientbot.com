━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  DATABASE REVIEW — ingredientbot.com
  Date: 2026-05-08 (Run 3)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  Found: 0 CRITICAL / 3 HIGH / 3 MEDIUM / 2 LOW
  Fixed: 6/8 (all CRITICAL/HIGH/MEDIUM fixed; LOW deferred)

  CRITICAL
  ━━━━━━━━
  None.

  HIGH
  ━━━━
  1. [MISSING_SELECT] src/app/(app)/kitchen/cook/[id]/page.tsx:14 — FIXED
     prisma.recipe.findFirst() with no select — loads rawText (db.Text), nutrition JSON,
     modifications JSON, and all other columns for cooking mode.
     Fix: Added select excluding rawText. Build-verified.

  2. [MISSING_SELECT] src/app/api/recipes/[id]/chat/route.ts:22 — FIXED
     prisma.recipe.findFirst() with no select — loads full recipe including rawText just
     to extract title, recipeData, and sourceIngredients for AI cooking chat context.
     Fix: Added targeted select (id, title, recipeData, sourceIngredients).

  3. [UNBOUNDED_QUERY] src/app/(app)/collections/[id]/page.tsx:12 — FIXED
     prisma.recipeCollection.findFirst() with include: { recipes: { ... } } and no take
     limit. A collection with 1000 recipes sends all to the client bundle.
     Fix: Added take: 200 cap with explanatory comment.

  MEDIUM
  ━━━━━━
  4. [MISSING_INDEX] prisma/schema.prisma — AICallLog.userId — FIXED
     AICallLog model has no index on userId field. Admin queries by user (e.g. cost
     attribution per user) would do a full table scan as the log grows.
     Fix: Added @@index([userId]) to AICallLog.

  5. [MISSING_SELECT] src/app/recipe/[id]/print/page.tsx:26 — FIXED
     Page component (not generateMetadata above it) calls findFirst() without select —
     loads rawText and modifications which the PrintRecipeView component doesn't use.
     Fix: Added select excluding rawText and modifications.

  6. [MISSING_SELECT] src/app/api/recipes/[id]/modify/route.ts:29 — FIXED
     findFirst() with no select. modify route passes recipe.rawText to AI prompt — rawText
     is needed, but other large fields (nutrition, tags, sourceIngredients, etc.) are not.
     Fix: Added select with rawText + only fields needed for AI prompting.

  LOW
  ━━━
  7. [MISSING_MAP] prisma/schema.prisma — AICallLog model — FIXED (schema only, needs db push)
     Table name "AICallLog" (PascalCase) inconsistent with all other models using snake_case
     via @@map. Applied @@map("ai_call_logs") — requires prisma db push to rename in DB.

  8. [MISSING_MAP] prisma/schema.prisma — RecipeCache model — FIXED (schema only, needs db push)
     Same issue — "RecipeCache" table name. Applied @@map("recipe_caches").

  MIGRATION SAFETY NOTES
  ━━━━━━━━━━━━━━━━━━━━━━
  - Last 3 migrations all use ADD COLUMN IF NOT EXISTS with DEFAULT values — no table-locking
    ALTER statements, no DROP statements, no column renames. Safe.
  - AICallLog and RecipeCache models were never created via a migration file (created via
    prisma db push per site's broken-migration-chain pattern). The @@map and @@index([userId])
    changes on AICallLog will need a prisma db push or a new migration to take effect in the DB.

  DEFERRED (not fixed)
  ━━━━━━━━━━━━━━━━━━━━
  - LOW issues: AICallLog @@map and RecipeCache @@map are schema-only fixes applied but
    require db push to rename tables. Not auto-applied to DB. Linear issues created.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
