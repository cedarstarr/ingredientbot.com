# /qa-e2e — ingredientbot.com — 2026-09-08

Project: chromium (desktop).

- Build: stale, rebuilt automatically.
- Reconcile: 2 findings → 1 real.
  - `sous-chef-answer` stale-testid is a FALSE POSITIVE: emitted at
    `src/components/kitchen/sous-chef.tsx:261` via a conditional spread
    `{...(isLatest ? { 'data-testid': 'sous-chef-answer' } : {})}`, invisible to the
    scanner's literal `data-testid` grep.
  - 1 real gap: `/change-password` had no spec. Written.
- New spec covers unauthenticated -> `/login` (@smoke) and authenticated -> panel renders.
  This page also has no "redirect out" branch; `middleware.ts:222` explicitly keeps it
  reachable during the email-verification gate. Form is not submitted — that would PATCH
  `/api/user/password` and rotate the shared staging test user's password.
- Playwright: 143/148 pass, 0 fail. Remaining gap is `test.skip` guards.
