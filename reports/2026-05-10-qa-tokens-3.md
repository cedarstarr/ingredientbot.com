# Token Hygiene Report — ingredientbot.com
**Date**: 2026-05-10  
**Run**: 4 (previous: 2026-05-08 run 2, 2026-05-10 run 3)

---

## Summary

| Category | Count |
|---|---|
| Hardcoded hex replaced | 7 (in global-error.tsx: 3 removed from inline styles) |
| Raw Tailwind palette replaced | 0 |
| Inline `style={{}}` blocks removed | 1 (global-error.tsx) |
| `dark:` variants added | 0 |
| `focus-visible:ring-2` rings added | 7 |

---

## Files Modified

| File | Changes |
|---|---|
| `src/app/global-error.tsx` | Removed all `style={{}}` inline blocks; replaced hardcoded hex (#0f0f0f, #f5f5f5, #a3a3a3, #c2613c) with Tailwind classes; added `focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2` to Try Again button |
| `src/components/app-nav.tsx` | Added `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring` to the close navigation button |
| `src/components/kitchen/cooking-mode-client.tsx` | Added `focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2` to: segmented progress-bar step buttons, timer reset button, timer play/pause button, skip step button, step dot buttons; added `focus-visible:ring-2 focus-visible:ring-ring` to ingredients toggle button and Exit cook mode link |
| `src/components/recipe/recipe-tags.tsx` | Added `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring` to autocomplete suggestion dropdown buttons |

---

## Audit Findings — Not Changed (Intentional)

| File | Violation | Reason Kept |
|---|---|---|
| `src/emails/*.tsx` | Inline styles with hardcoded hex (#e57c2c, #111, #555, #999) | React Email requires inline styles — email clients don't load CSS classes. All hex matches brand palette. |
| `src/app/opengraph-image.tsx` | Hardcoded colors (if any) | Satori cannot read CSS variables — explicitly excluded per task spec. |
| `src/components/recipe/print-recipe-view.tsx` | `print:text-black`, `print:text-gray-*`, `print:border-gray-*` | Print media queries require concrete grayscale ink values. File has `// design-system-exempt` comment. |
| `src/components/kitchen/cooking-mode-client.tsx` | `bg-white/15`, `bg-white/10`, `text-white`, `border-white/20`, etc. | Full-screen dark cooking mode uses white-at-opacity overlay pattern for glass-morphism. These are alpha-channel variants, not raw palette swaps. |

---

## Manual Review Items

| File | Item |
|---|---|
| `src/app/global-error.tsx` | `bg-[#0f0f0f]`, `text-[#f5f5f5]`, `text-[#a3a3a3]` remain as hardcoded Tailwind bracket values. **This is correct**: `global-error.tsx` replaces the entire document and renders before CSS variables or the theme provider are loaded. Semantic tokens (`bg-background`, `text-foreground`) won't resolve here. These are intentional dark-mode fallback values. |

---

## Verification

```
npx prisma generate && npx tsc --noEmit → clean (0 errors)
```

Self-review grep for `text-gray-`, `bg-white`, `bg-slate-`, `text-black`, `text-[#`, `bg-[#` in non-exempt files: only `global-error.tsx` bracket values remain (see manual review above).
