━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  PERFORMANCE AUDIT — ingredientbot.com
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  Date:       2026-07-02
  Branch:     staging
  Next.js:    16.2.7 (App Router)
  Mode:       static analysis only (no Lighthouse, no dev server)

  Bundle:     0 issues
  Images:     0 issues
  Caching:    0 issues
  Web Vitals: 0 issues
  Database:   0 issues

  TOTAL: 0 issues found — no fixes applied.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  PHASE DETAIL
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  Phase 1 — Bundle Analysis
  ─────────────────────────
  - 9 `"use client"` components (ui primitives) + ~40 leaf client components.
    Spot-checked providers.tsx, recipe-tags.tsx, sw-register.tsx and others:
    every "use client" file genuinely uses hooks/handlers/context providers.
    No unnecessary "use client" directives.
  - No heavy client-imported packages (no chart.js/recharts/d3/lodash/moment/
    date-fns-full/@dnd-kit) in package.json. Nothing needs next/dynamic.
  - No barrel imports from @/lib, @/components, or @/hooks — all imports are
    direct paths.

  Phase 2 — Image Optimization
  ────────────────────────────
  - No raw <img> tags anywhere in src/.
  - No <Image> usages either (site is icon/typography-driven; lucide-react only).
  - No background-image / url(http...) inline images.
  Nothing to optimize.

  Phase 3 — Caching & Data Fetching
  ─────────────────────────────────
  - Public route src/app/r/[slug]/page.tsx already has `export const revalidate
    = 3600` and uses React cache() to dedupe the DB read between generateMetadata
    and render.
  - Remaining dynamic routes (recipe/[id], collections/[id], kitchen/cook/[id])
    are per-user authenticated pages — generateStaticParams is NOT appropriate
    (user-scoped data cannot be pre-rendered). The public r/[slug] route relies
    on ISR rather than generateStaticParams by design (slugs created after build
    are handled by revalidate); this is correct, not a finding.
  - No unnecessary { cache: 'no-store' } on stable content.
  - All app-level findMany calls are bounded: sitemap (select + take:1000),
    admin lists (take:100/200), listing pages (take:PAGE_SIZE or take:500),
    crons (date-windowed + safety take:5000). Every call uses `select` or a
    scoped `include`. No unbounded / SELECT-* queries.

  Phase 4 — Core Web Vitals Patterns
  ──────────────────────────────────
  - Fonts: next/font/google (Inter) in layout.tsx. No render-blocking CSS
    @import url() font imports.
  - The two <script> tags (app/page.tsx, r/[slug]/page.tsx) are
    application/ld+json structured-data blocks (SEO), not blocking JS.
  - No third-party analytics/chat scripts (gtag/hotjar/intercom/crisp/fbq).
  - No styled-components / @emotion runtime CSS (Tailwind, zero-runtime).

  Phase 5 — Database Performance
  ──────────────────────────────
  - Pooling handled via env DATABASE_URL (Neon staging / Railway prod) — schema
    datasource uses env, no inline url. Pooling is an infra concern, not a code
    finding.
  - Aggregations (count/aggregate) are all userId-scoped, backed by indexes.
  - Sequential-query hotspots already parallelized with Promise.all across
    dashboard, recipe/[id], and other pages (verified). The dashboard streak
    query is deliberately capped at 2 years (take:2000) with a decision-log
    comment explaining the bound.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  NOTES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  This codebase is already in excellent performance shape. The previous
  /qa-perf run (2026-05-04) resolved the outstanding HIGH/MEDIUM findings
  (removed unnecessary "use client" from collection-detail + cooking-heatmap,
  parallelized three sequential Prisma pairs). This run finds the tree clean.

  Next.js 16 note: no next/dynamic({ssr:false}) fix was needed anywhere; the
  site has no heavy client-only chart/editor components to lazy-load.

  Build verification: not run — 0 source files modified.
