# /qa-e2e — ingredientbot.com — 2026-08-17

Part of a full-portfolio run across all 14 repos, desktop chromium project.

## Result

- **Playwright:** 110/110 passed, **0 failures**, 56s
- **Reconcile:** in sync (33 routes, 22 specs)
- **Build:** rebuilt before the run (source was newer than `.next/`)

## Changes

None — no edits were needed in this repo.

## Portfolio context

14 sites, 1,978 tests, 1,946 passed. Two genuine failures found portfolio-wide, both fixed
and re-verified green:

- **padhr.com** — `notFound()` returned HTTP 200 instead of 404. A group-level `loading.tsx`
  added by FOU-232 flushed a 200 shell before the page component could throw, re-introducing
  the soft-404 that the FOU-219 sweep had cleared. Two public routes affected.
- **dailypriceguru.com** — a test read `<main>` mid-stream, after the loading fallback was
  removed but before content landed, so it saw neither cards nor the empty state.

A follow-up sweep of the other 12 repos found no further public soft-404 routes; six gated
ones are deferred as FOU-410.

Of 13 reconcile findings raised across 3 sites, **all 13 were false positives** — the scanner
cannot resolve testids passed as a `testId` prop, testids built from template literals, or
routes covered by a slug loop. Filed as FOU-408.

## Issues filed

- FOU-408 — qa-spec-reconcile false positives (three distinct blind spots)
- FOU-409 — dailypriceguru flaky TCO calculator test
- FOU-410 — soft-404 on 6 gated dynamic routes (deferred, no SEO impact)
