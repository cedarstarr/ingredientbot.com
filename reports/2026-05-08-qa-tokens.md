# QA Tokens — Hygiene Pass

**Date:** 2026-05-08
**Branch:** staging
**Site:** ingredientbot.com

## Summary

Mechanical token-hygiene pass. Fixed raw-palette grayscale leaking through to the screen viewport in the print recipe view, and replaced a `text-white` checkmark with `text-primary-foreground` in the recipe detail. No aesthetic changes.

## Files Modified

- `src/components/recipe/print-recipe-view.tsx` — 4 edits
- `src/components/recipe/recipe-detail-client.tsx` — 1 edit

## Counts

| Metric | Count |
|---|---|
| Hardcoded hex replaced | 0 |
| Raw palette classes replaced | 6 |
| Dark variants added | 0 (all targets use semantic tokens that already adapt) |
| Focus-visible rings added | 0 (existing primitives already include them) |
| Inline styles removed | 0 |

### Detail of replacements

- `border-gray-900` (screen) → `border-foreground` (kept `print:border-gray-800` as the print-fidelity value)
- `border-gray-900` on nutrition card → `border-foreground` (kept `print:border-gray-800`)
- `border-gray-300 pb-2` (screen, no print prefix) → `border-border print:border-gray-300 pb-2`
- `border-gray-200 pb-1.5` → `border-border print:border-gray-200 pb-1.5`
- `bg-gray-900 text-white print:bg-gray-800` → `bg-foreground text-background print:bg-gray-800 print:text-white`
- `text-white` checkmark inside `bg-primary` checkbox → `text-primary-foreground`

## Manual Review (Skipped — Intentional)

These were inspected and left alone. No changes recommended unless aesthetic intent changes.

1. **`src/components/kitchen/cooking-mode-client.tsx`** — fullscreen dark cooking-mode overlay. Uses `text-white`, `bg-white/N`, `border-white/N` opacity overlays on a dark wrapper. Wrapper at `src/app/(app)/kitchen/cook/layout.tsx` uses `bg-[oklch(0.14_0.01_50)]`. Both are deliberate dark-overlay aesthetic. If this hardcoded dark color should adopt a token (e.g. `--cook-bg`), surface to design — not a mechanical fix.
2. **`src/app/global-error.tsx`** — Tailwind doesn't run on the global-error boundary, so inline styles with hex are required. Skip.
3. **`src/emails/*.tsx`, `src/lib/email.ts`, `src/app/api/cron/meal-plan-digest/route.ts`** — email HTML / cron HTML. Email clients can't read CSS vars; hex is the right choice. Skip.
4. **`src/components/recipe/cooked-this-button.tsx`** — `text-white` paired with the success token `bg-[hsl(var(--color-success))]`; intentional contrast pair. Token-shaped already.
5. **Print exempt comments** — `print-recipe-view.tsx` is marked `design-system-exempt` for print: classes; only the screen-side classes were touched.
6. **Collection color picker** — `style={{ color: collection.color }}` in collection-detail/picker/history is a user-chosen dynamic color (allowed dynamic style).

## Verification

- `npx prisma generate` — pass
- `npx tsc --noEmit` — pass (no errors)
- Re-grep of modified files: clean of `text-gray-`, `bg-white`, `bg-slate-`, `text-black`, `text-[#`, `bg-[#` outside intentional surfaces
