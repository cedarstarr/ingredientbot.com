# Tests Fix — 2026-05-08

**Project:** chromium (desktop)
**Result:** ✅ 63/63 passing after 1 round

## Initial run
- 58 passed / 5 failed (1.6m)

## Failures fixed (5 tests)

| Tier | Test | Root cause | Fix |
|------|------|------------|-----|
| 1 (Auth) | `pantry-journey.spec.ts:53` — anon /pantry redirect | describe-level `beforeEach` already authenticated the page before "anon" test ran | Spawn fresh `browser.newContext()` for the unauthenticated test |
| 1 (Auth) | `settings-dietary.spec.ts:103` — public share `/r/[slug]` nav | `/r/` not in `PUBLIC_PATHS` of `src/middleware.ts` — was redirecting to /login | Added `/r/` to PUBLIC_PATHS |
| 3 (Core) | `kitchen-advanced.spec.ts:141` — Spice level slider | Radix Slider Thumb has `role="slider"` but `aria-label` is on the Root | Added `data-testid="spice-level-slider"` on Slider; test uses testid |
| 3 (Core) | `settings-dietary.spec.ts:13` — Save Preferences button | Two "Save Preferences" buttons exist (notifications + dietary), strict-mode violation | Added `data-testid="dietary-save-preferences"` on dietary save |
| 3 (Core) | `settings-dietary.spec.ts:48` — medical dietary flags | `getByLabel(/low-sodium/i)` matched both visible Checkbox span and hidden base-ui input clone | Added `data-testid` on each medical-flag label wrapper |

## Fix applied
- **R1 commit `31c7d38`** — pushed to `origin/staging`.

## Final retest
- 63 passed (1.4m) — clean.

## Files modified
- `src/middleware.ts`
- `src/components/kitchen/kitchen-panel.tsx`
- `src/components/settings/dietary-profile-section.tsx`
- `tests/kitchen-advanced.spec.ts`
- `tests/pantry-journey.spec.ts`
- `tests/settings-dietary.spec.ts`

## Notes
- Project memory note about `src/proxy.ts` is stale — site uses `src/middleware.ts`. Confirmed via repo check.

## Remaining failures
None.
